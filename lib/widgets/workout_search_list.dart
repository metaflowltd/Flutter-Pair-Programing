import 'package:flutter/material.dart';


class WorkoutSearchList extends StatelessWidget {
  WorkoutSearchList({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text("Display the list here");
  }
}
